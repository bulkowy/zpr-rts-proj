Import('env')
Import('target_path')

server = env.Program(target_path + 'server', [Glob('*.cpp'), Glob('../game/*.cpp')])
#libserver = env.StaticLibrary(target_path + '/test/libserver', [Glob('*.cpp'), Glob('../game/*.cpp')])
#tests = env.BoostTests(Glob("../tests/server/*.cpp"), libserver)